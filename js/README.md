# Legacy JavaScript Files

**These files are no longer used in the application.**

The application has been migrated to Svelte. The new code structure is:
- `src/stores/auth.js` - Replaces `js/auth.js`
- `src/stores/storage.js` - Replaces `js/storage.js`
- `src/components/*.svelte` - Replaces `js/ui.js` and `js/app.js`

These legacy files are kept for reference only and can be removed in a future cleanup.
